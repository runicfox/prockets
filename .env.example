# Server
PORT=8080
NODE_ENV=development
# CORS – comma-separated list of allowed origins (dashboard, extension, etc.)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,chrome-extension://your-extension-id

# JWT
JWT_ACCESS_SECRET=replace_with_strong_random_32b
JWT_REFRESH_SECRET=replace_with_strong_random_32b
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Cookies
COOKIE_NAME=refresh_token
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
COOKIE_SAMESITE=Lax

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pocketauth?schema=public
